export const Bamboo = ({ id, domain, taskName, start, end, memos }) => {
    return (
        <div className="w-full h-20 text-white border-2 border-white">
            {taskName}
        </div>
    );
};
